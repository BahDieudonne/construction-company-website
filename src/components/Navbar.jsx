import {useState} from 'react';import {Link,NavLink} from 'react-router-dom';import {company} from '../config/company';
const links=[['/','Home'],['/about','About'],['/services','Services'],['/projects','Projects'],['/products','Products'],['/industries','Industries'],['/blog','Blog'],['/careers','Careers'],['/contact','Contact']];
export default function Navbar(){const [open,setOpen]=useState(false);return <header className="site-header"><div className="container nav-wrap"><Link to="/" className="brand" onClick={()=>setOpen(false)}><img src={company.logo} alt={`${company.companyName} logo`}/></Link><nav className={open?'nav open':'nav'} aria-label="Primary navigation">{links.map(([to,label])=><NavLink key={to} to={to} end={to==='/'} onClick={()=>setOpen(false)}>{label}</NavLink>)}<Link className="nav-cta" to="/request-quote" onClick={()=>setOpen(false)}>Request a Quote</Link></nav><button className="menu-btn" aria-label="Toggle navigation" aria-expanded={open} onClick={()=>setOpen(!open)}><span/><span/><span/></button></div></header>}
